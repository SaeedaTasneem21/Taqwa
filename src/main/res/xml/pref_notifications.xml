<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <SwitchPreference
        android:key="notifications_prayer_time"
        android:title="@string/pref_title_prayer_time"
        android:defaultValue="true" />


    <!--  Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- ListPreference's summary should be set to its value by the activity code. -->


    <!--  This preference will be enabled only when the checkbox above is checked. -->
    <SwitchPreference
        android:dependency="notifications_prayer_time"
        android:key="notifications_vibrate"
        android:title="@string/pref_title_vibrate"
        android:defaultValue="true" />
</PreferenceScreen>